
"use client";

import React from 'react';

/**
 * Legacy Navbar repurposed as a mobile-only top trigger bar
 * or exported as empty since AppSidebar is now the main navigation.
 */
export const Navbar = () => {
  return null; // Side Navigation is now handled by AppSidebar in RootLayout
};
